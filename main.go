package main

func main() {

	// var card string = "Ace of spades"

	// card := newCard()
	// We assigned a string (could be any data type)
	// to variable by :=

	// card = "Five of diamonds"
	// No need to tell again
	// about := just = after once

	// fmt.Println(card)

	cards := newDeck()
	// Slice data type

	cards.print()

}
